
# Local IDE / editor state
.obsidian/
.vscode/

# Runtime analytics (session logs, not shippable)
grimoires/loa/analytics/

# Run mode state (session-local, not shippable)
.run/

# Local Claude Code settings (permissions, not shippable)
.claude/settings.local.json
.claude/.mount-lock

# Local task tracking state
.beads/

# Framework version metadata
.loa-version.json

# LOA SUBMODULE SYMLINKS (added by mount-submodule.sh)
# Symlinks to .loa/ submodule — recreated on mount
.claude/scripts
.claude/protocols
.claude/hooks
.claude/data
.claude/schemas
.claude/loa/CLAUDE.loa.md
.claude/loa/reference
.claude/loa/feedback-ontology.yaml
.claude/loa/learnings
.claude/settings.json
.claude/checksums.json
.loa-state/
.claude.backup.*
