# Code Hygiene Report

> Generated by /ride — 2026-06-09. Items flagged for HUMAN DECISION. Nothing auto-fixed.

## Summary

| Category | Items Flagged |
|----------|---------------|
| Untracked build artifacts | 1 |
| Temp files | 1 |
| Missing referenced files | 1 |
| Gitignore gaps | 1 |
| CI coverage gaps | 2 |
| Stale comments/markers | 2 |

## Findings

### H1: `__pycache__/` not gitignored
- **Evidence**: `domains/character-voice/scripts/__pycache__/` is untracked (git status) and `.gitignore` has no `__pycache__` or `*.pyc` pattern
- **Risk**: Accidental commit of Python bytecode
- **Suggested action**: add `__pycache__/` and `*.pyc` to `.gitignore`

### H2: Stray temp file `grimoires/loa/NOTES.md.tmp`
- **Evidence**: untracked file in State Zone (git status)
- **Suggested action**: inspect and delete or merge into NOTES.md

### H3: README references nonexistent LICENSE
- **Evidence**: `README.md:79` says "See LICENSE file" — no `LICENSE*` file exists at repo root
- **Risk**: Consumers of a public construct have no license terms (repo references 0xHoneyJar org / external consumers)
- **Suggested action**: add a LICENSE file or remove the reference

### H4: character-voice schemas not validated in CI
- **Evidence**: `scripts/ci/validate-schemas.sh:7-8` only covers `schemas/core` + `domains/ttrpg/schemas`; no `character-voice` reference anywhere in `scripts/ci/` or `.github/workflows/ci.yaml`
- **Risk**: The 3 character-voice schemas (voice-character, session-events-character, digest-character) can drift without CI failure
- **Suggested action**: extend validate-schemas.sh to iterate all `domains/*/schemas/`

### H5: freeside adapter round-trip test not in CI
- **Evidence**: `domains/character-voice/scripts/test-roundtrip.sh` exists and passes locally (verified 2026-06-09, exit 0, 9/9 sync markers) but is not invoked by `.github/workflows/ci.yaml`
- **Risk**: adapter regressions ship silently
- **Suggested action**: add a step to the `arneson-alone` CI job

### H6: Stale "v2" header comment in construct.yaml
- **Evidence**: `construct.yaml:1` says "construct-arneson v2"; `construct.yaml:12` says `version: 3.3.0`
- **Suggested action**: update comment or drop the version from it

### H7: CI Gygax stub still marked "Sprint 7 will replace"
- **Evidence**: `.github/workflows/ci.yaml` (arneson-with-gygax job) sets up a stub with comment "Sprint 7 will replace this with a real Gygax v3 installation"
- **Status**: intentional WIP marker from the v2 cycle; composition CI still runs against stub
- **Suggested action**: confirm whether real-Gygax composition CI is still planned

## Dead Code Assessment

No commented-out code blocks found in Python/shell sources. No TODO/FIXME/HACK/XXX markers found anywhere in the app zone (clean). No files outside standard directories.
