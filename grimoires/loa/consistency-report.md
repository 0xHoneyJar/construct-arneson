# Consistency Report

> Generated by /ride — 2026-06-09. Analysis only — no changes made. Breaking changes flagged for human decision.

## Consistency Score: 9 / 10

## Naming Patterns

### Schema files — pattern: `{kind}-{qualifier}.schema.yaml` (kebab-case)

| File | Conforms |
|------|----------|
| `voice-base`, `session-events-base`, `digest-base`, `safety` (core) | Yes |
| `voice-{archetype,npc,pc}`, `session-events-ttrpg`, `digest-ttrpg` (ttrpg) | Yes |
| `voice-character`, `session-events-character`, `digest-character` (character-voice) | Yes |
| `voice-test`, `session-events-test`, `digest-test` (test-domain) | Yes |
| **`experiential_intent.schema.yaml`** (core) | **No — snake_case outlier** (`schemas/core/experiential_intent.schema.yaml`) |

**CONFLICT C1**: `experiential_intent` is the only snake_case schema filename among 16 schemas. Internal header comment (`experiential_intent.schema.yaml:1`) even cites itself as `schemas/experiential_intent.schema.yaml` (missing the `core/` segment — a second minor inconsistency). Renaming to `experiential-intent.schema.yaml` would be a **breaking change**: the path is referenced in `construct.yaml:83` and CI smoke test (`.github/workflows/ci.yaml` v2 structure test). Flagged, not fixed.

### Skills — pattern: `{skill}/index.yaml` + `{skill}/SKILL.md`

All 9 skills (3 core, 5 ttrpg, 1 test-domain) follow the pattern. Each index.yaml carries `name`, `description`, PRD FR reference comment, and protocol declarations. Fully consistent.

### Scripts

- Python: snake_case (`ingest_persona.py`, `emit_persona.py`) — consistent
- Shell: kebab-case (`test-roundtrip.sh`, `validate-*.sh`) — consistent per-language convention

### Directories

`domains/{domain}/{schemas,skills,resources,scripts,adapters}` — consistent across ttrpg, character-voice, and test-domain. character-voice adds `adapters/` and `scripts/` (documented in `domain.conventions.md:117-121`).

## Versioning Consistency

| Surface | Version | Note |
|---------|---------|------|
| `construct.yaml:12` | 3.3.0 | manifest version |
| `construct.yaml:1` comment | "v2" | **stale** (DRIFT-4) |
| PRD/SDD/sprint.md | v3.4 | next cycle, planned + implemented |
| Schema versions | base schemas v2, domain schemas v1, safety/experiential_intent v1 | internally coherent |

**OPPORTUNITY O1**: bump `construct.yaml` version to 3.4.0 when the v3.4 adapter cycle merges to main, and fix the v2 header comment.

## Cross-Reference Consistency

- All skill index.yaml protocol lists reference the 4 declared core protocols; none reference `anti-patterns` or `meta-interactions` even though anti-patterns is mandatory for all personas (`protocols/anti-patterns.md:3` "Applied to ALL personas"). **OPPORTUNITY O2**: declare the two v3 protocols in construct.yaml and (optionally) in skill index protocol lists, mirroring how test-workshop validates protocol references in CI.
- PRD FR references in skill index.yaml comments (FR-1..FR-10) trace to PRD v2 numbering while current prd.md is the v3.4 delta with its own FR-1..FR-5. Not a conflict (different cycles) but worth a v-prefix convention (e.g., `PRD-v2 FR-6`). **OPPORTUNITY O3**.

## Improvement Opportunities (no action taken)

| ID | Item | Breaking? |
|----|------|-----------|
| C1 | Rename `experiential_intent.schema.yaml` → kebab-case | YES — construct.yaml + CI references |
| O1 | Version bump + header comment fix | No |
| O2 | Declare anti-patterns + meta-interactions in construct.yaml | No |
| O3 | Version-qualify PRD FR references in skill index comments | No |
